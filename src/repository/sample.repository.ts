
class SampleRepo {

    // async getPartyEntry(condition: any = {}): Promise<PartiesFields | null> {
    //     return new Promise(async (resolve, reject) => {
    //         try {
    //             const response = await getPartiesModel().findOne(condition);
    //             resolve(response);
    //         } catch (err) {
    //             reject(err);
    //         }
    //     });
    // }

}

export default new SampleRepo();
